# Foto interna do relatório (não publicar no site)
_docs/

# Sistema
Thumbs.db
.DS_Store
node_modules/

# Compiled from src/**/*.ts by `npm run build` — a GitHub Actions workflow builds it
# fresh and deploys it to GitHub Pages, so the compiled output never needs to be committed.
build/

.vscode
.claude

